[project]
name = "veilleur"
version = "0.1.0"
description = "Automated LinkedIn tech watch article generation"
requires-python = ">=3.11"
dependencies = [
    "chromadb>=1.0.0",
    "google-genai>=1.0.0",
    "Pillow>=11.0.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
